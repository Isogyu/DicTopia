# DicTopia（ディクトピア）テスト計画書

| 項目 | 内容 |
|---|---|
| 文書種別 | テスト計画書 |
| 対象プロジェクト | DicTopia（ディクトピア） |
| バージョン | v1.0 |
| 作成日 | 2026年8月16日 |
| 前提文書 | システム仕様書 v1.0／詳細設計書 v1.0 |

---

## 1. テストの目的・方針

- 投稿・投票・通報・OGP生成という**低摩擦かつ不正防止が重要な4つのコア機能**を最優先でテストする。
- AIモデレーション（OpenAI API）やSupabaseなど**外部依存はモック化**し、テストの再現性と実行速度を確保する。
- 「同日重複投票の防止」「通報3件到達時の自動非公開」など、**ビジネスルールの境界値**を重点的に検証する。
- E2Eテストは「投稿→表示→投票→シェア」という**ユーザーの主要導線を最優先**でカバーする。

---

## 2. テストレベルと使用ツール

| レベル | 対象範囲 | 使用ツール |
|---|---|---|
| 単体テスト（Unit） | 純粋関数・ユーティリティ・個別コンポーネント | Vitest + React Testing Library |
| 結合テスト（Integration/API） | Route Handler単位（DB・外部APIはモック） | Vitest + `msw`（APIモック） |
| E2Eテスト | ブラウザ操作を伴う一連のユーザーフロー | Playwright |
| 型チェック | TypeScript Strict Modeによる静的検証 | `tsc --noEmit`（CI必須項目） |
| Lint | コード品質・アクセシビリティ基礎 | ESLint（`eslint-plugin-jsx-a11y`含む） |

---

## 3. テスト環境・データ戦略

| 対象 | 方針 |
|---|---|
| Supabase | ローカルはSupabase CLI（`supabase start`）によるDockerローカルDBを使用し、`supabase/migrations/0001_init.sql` を適用したクリーンな状態からテストする。E2Eは専用のテスト用プロジェクト（本番と分離）を使用。 |
| OpenAI Moderation API | 単体・結合テストでは `msw` でHTTPレスポンスをモック（`flagged: true/false` の両パターンを用意）。E2Eの一部シナリオのみ実APIを許容（コスト・安定性を考慮し最小限に限定）。 |
| gpt-4o-mini（SEOエンリッチメント） | 非同期・非クリティカル処理のため、テストでは呼び出しの有無と失敗時にメイン処理を止めないことのみ検証（レスポンス内容の厳密検証はしない）。 |
| voter_hash / reporter_hash | テスト内では固定のIP・User-Agent・日付を注入し、ハッシュ生成関数を決定論的にテストする。 |
| テストデータ投入 | `supabase/seed.sql` にテスト用の造語・お題を用意し、E2E実行前にリセット＋再投入するスクリプトを用意する。 |

---

## 4. 単体テスト（Unit Test）計画

### 4.1 ユーティリティ関数

| 対象ファイル | テストケース |
|---|---|
| `lib/hash.ts` | ・同一入力（IP+UA+日付）から常に同じハッシュが生成される<br>・日付が変わるとハッシュが変化する<br>・IPまたはUAが異なるとハッシュが変化する |
| `lib/week.ts` | ・現在時刻からJST基準で正しい `week_code`（例: `2026-W33`）が算出される<br>・日曜23:59 JST直前/直後で週が正しく切り替わる<br>・カウントダウン残り時間の計算が正しい |
| `lib/validation.ts`（zodスキーマ） | ・`word` が31文字以上でバリデーションエラーになる<br>・`definition` が201文字以上でバリデーションエラーになる<br>・空文字の `word`/`definition` がエラーになる<br>・`example_sentence` は空でも許可される |
| `lib/openai/moderation.ts` | ・`flagged: true` レスポンスを正しく解釈し拒否判定を返す<br>・API呼び出し失敗時に例外を握りつぶさず呼び出し元に伝播する |

### 4.2 コンポーネント単体テスト

| 対象コンポーネント | テストケース |
|---|---|
| `VoteButton` | ・初期表示で `initialCount` が表示される<br>・クリック時に楽観的に+1表示される<br>・APIが429を返した場合、表示がロールバックされ無効化される<br>・連打（二重クリック）してもリクエストが1回しか送られない（デバウンス/ローディング状態制御） |
| `ReactionBar` | ・4種の絵文字ボタンが表示される<br>・クリックでカウントが即座に増える（楽観的更新） |
| `SubmissionModal` | ・`word` 入力が30文字を超えると送信ボタンが無効化され、残り文字数が正しく表示される<br>・`definition` が200文字を超えるとエラーメッセージが表示される<br>・必須項目未入力時に送信できない<br>・送信成功時に `onSubmitted` が呼ばれ、モーダルが閉じる |
| `HeroTopicCard` | ・`activeTopic` がnullの場合、フォールバック表示がされる<br>・カウントダウンが1秒ごとに更新される |
| `WordCard` | ・`variant="leaderboard"` の場合のみ順位番号が表示される<br>・通報アイコンクリックで確認導線が表示される |

---

## 5. 結合テスト（API / Route Handler）計画

### 5.1 POST /api/words

| # | シナリオ | 期待結果 |
|---|---|---|
| 1 | 正常な word/definition で投稿 | `201`、DBに `is_published: true` でレコードが作成される |
| 2 | モデレーションAPIが `flagged: true` を返す入力 | `422`、固定エラーメッセージが返る。DBにレコードが作成されない |
| 3 | `word` が31文字 | `400`、バリデーションエラー |
| 4 | `definition` が201文字 | `400`、バリデーションエラー |
| 5 | `topic_id` に存在しないUUIDを指定 | 仕様上 `ON DELETE SET NULL` のFKであるため、存在しないIDの扱いを明確化しテスト（外部キー制約違反として400を返す設計とする） |
| 6 | 投稿成功後、非同期でSEOタグ生成が呼ばれる | `ai_context_tags`・`ai_search_summary` が後続で更新される（モック関数の呼び出し確認） |
| 7 | SEOタグ生成（gpt-4o-mini）呼び出しが失敗する | メインの投稿処理（201レスポンス）には影響しない |
| 8 | OpenAI Moderation APIがタイムアウト/エラーを返す | `500`、投稿は作成されない（フェイルクローズ方針、詳細設計書9章で確定済み） |

### 5.2 POST /api/words/[id]/vote

| # | シナリオ | 期待結果 |
|---|---|---|
| 1 | 初回投票 | `200`、`votes_count` が1増加 |
| 2 | 同一IP・UA・同日に再度投票 | `429`、固定エラーメッセージ。`votes_count` は増加しない |
| 3 | 同一IP・UAで日付が変わって投票 | `200`、正常に投票できる（`voter_hash` が変化するため） |
| 4 | 存在しない `word_id` | `404` |
| 5 | 同時多重リクエスト（同一ユーザーが連続送信） | `votes_count` の二重加算が発生しない（DBのUNIQUE制約＋アトミックなRPCで担保されていることを確認） |

### 5.3 POST /api/words/[id]/react

| # | シナリオ | 期待結果 |
|---|---|---|
| 1 | 有効な `emoji_type`（fire/laugh/cry/clap）で送信 | `201`、reactionsテーブルにレコード追加 |
| 2 | 不正な `emoji_type`（例: `"angry"`） | `400` |
| 3 | 存在しない `word_id` | `404` |

### 5.4 POST /api/words/[id]/report

| # | シナリオ | 期待結果 |
|---|---|---|
| 1 | 1件目の通報 | `201`、`reports_count = 1`、`is_published` は `true` のまま |
| 2 | 3件目の通報（境界値） | `201`、`reports_count = 3`、`is_published` が自動的に `false` に更新される |
| 3 | 既に非公開の造語への追加通報 | `201`、`reports_count` は増加するが、再公開はされない |
| 4 | 存在しない `word_id` | `404` |
| 5 | 同時多重リクエスト（複数ユーザーがほぼ同時に通報） | `increment_reports_count` RPCのアトミック性により二重加算が起きず、3件到達時のみ `auto_unpublished: true` になる |

### 5.5 GET /api/og/word/[id]

| # | シナリオ | 期待結果 |
|---|---|---|
| 1 | 存在する造語ID | `200`、`image/png`、1200×630サイズの画像が返る |
| 2 | 存在しない造語ID | フォールバック画像（DicTopiaロゴのみ）が `200` で返る |
| 3 | `definition` が120文字を超える場合 | 画像内テキストが120文字で切り詰められる |

---

## 6. E2Eテスト計画（Playwright）

### 6.1 優先度: 最高（Critical Path）

| # | シナリオ名 | 手順概要 | 期待結果 |
|---|---|---|---|
| E2E-1 | 造語投稿〜表示の一連の流れ | ホーム表示→「造語を作る」クリック→フォーム入力→送信 | モーダルが閉じ、トースト表示、新着造語リストに反映される |
| E2E-2 | 投票の一連の流れ | ホームで造語カードの投票ボタンをクリック | 投票数が即座に+1表示され、再読み込み後も反映されている |
| E2E-3 | 同日重複投票のブロック | 同一ブラウザセッションで同じ造語に2回投票を試みる | 2回目は「本日はこの造語にすでに投票済みです」が表示され、カウントは増えない |
| E2E-4 | 造語個別ページ表示 | ホームから造語カードをクリックし個別ページへ遷移 | タイトル・OGPメタタグ・JSON-LDが正しく出力されている（HTML検証） |
| E2E-5 | 不適切ワードの投稿拒否 | モデレーションでflagされることが既知のテスト文字列を投稿 | フォームにエラーが表示され、一覧に追加されない |

### 6.2 優先度: 高

| # | シナリオ名 | 手順概要 | 期待結果 |
|---|---|---|---|
| E2E-6 | 絵文字リアクション | 造語カードで🔥をクリック | カウントが即座に増える |
| E2E-7 | 通報フローと自動非公開 | テスト用造語に対し3件通報（3ブラウザコンテキストで擬似的に別ユーザーを再現） | 3件目通報後、一覧から当該造語が消える（`is_published=false`が反映） |
| E2E-8 | Xシェアボタン | 造語個別ページで「Xでシェア」クリック | 正しいOGP画像URLと`#DicTopia`を含むツイートインテントURLが生成される（新規タブのURLパラメータを検証） |
| E2E-9 | 殿堂入りページ表示 | `/hall-of-fame` へ遷移 | `week_code` ごとにグルーピングされた過去1位ワードが表示される |

### 6.3 優先度: 中（レスポンシブ・UI）

| # | シナリオ名 | 手順概要 | 期待結果 |
|---|---|---|---|
| E2E-10 | モバイル表示崩れ確認 | ビューポート375px幅でホーム・個別ページを表示 | レイアウト崩れがない（Playwrightのスクリーンショット比較） |
| E2E-11 | ダークテーマの視認性 | 全ページでコントラスト・配色を確認 | 主要テキストが背景に対し十分なコントラストを持つ |
| E2E-12 | カウントダウンタイマー表示 | HeroTopicCardの週次リセットカウントダウンを確認 | 秒単位で減少表示される |

---

## 7. 非機能テスト

| 種別 | 内容 | 手法 |
|---|---|---|
| パフォーマンス | 造語個別ページの初期表示速度（ISR効果の確認） | Lighthouse CIをCIパイプラインに組み込み、主要指標（LCP等）を計測 |
| セキュリティ | RLSにより未公開（`is_published=false`）の造語がクライアントから取得できないことを確認 | Supabase匿名キーを用いた直接クエリテスト（結合テストに含める） |
| セキュリティ | 投票・通報APIへの過剰リクエスト（連打）に対する挙動確認 | 結合テストの5.2-#5に含める |
| アクセシビリティ | フォーム・ボタンへのキーボード操作、スクリーンリーダー用ラベル | axe-core（`@axe-core/playwright`）をE2Eに組み込み |
| SEO | JSON-LD・メタタグの構文妥当性 | 結合テストでHTML出力をパースし、`DefinedTerm` スキーマの必須フィールドが揃っているか検証 |

---

## 8. カバレッジ目標・完了基準（Exit Criteria）

| 項目 | 目標 |
|---|---|
| 単体テストのラインカバレッジ | `lib/` 配下 90%以上、`components/` 配下 70%以上 |
| 結合テスト | 全APIエンドポイントの正常系・主要異常系（表内の全項目）を100%網羅 |
| E2Eテスト | 優先度「最高」（E2E-1〜5）は全てPassが必須。「高」「中」はPass率90%以上を目標とし、既知の失敗は理由を記録の上リリース判断者が承認 |
| CI必須ゲート | `tsc --noEmit`、ESLint、単体テスト、結合テストがすべてGreenであることをマージ条件とする（E2Eはstaging環境へのデプロイ後に実行し、必須ゲートとはしない） |

---

## 9. CI/CDへの組み込み

| ステージ | 実行タイミング | 内容 |
|---|---|---|
| PR作成時 | GitHub Actions | 型チェック → Lint → 単体テスト → 結合テスト |
| main/staging反映時 | デプロイ後（Vercel Preview環境） | Playwright E2E（優先度「最高」のみ自動実行） |
| 週次 | 定期実行 | E2Eフルセット（優先度「中」まで含む）＋Lighthouse CI |

---

## 10. リスクベースの優先順位付け（テスト実施順）

1. **投票の重複防止ロジック**（不正防止の根幹、`voter_hash`のUNIQUE制約） — 最優先
2. **モデレーション拒否フロー**（不適切コンテンツの流入防止） — 最優先
3. **通報3件到達時の自動非公開**（ビジネスルールの境界値） — 高
4. **OGP画像生成の正確性**（バイラル拡散の起点、崩れると成長戦略に直結） — 高
5. **SEOメタデータ/JSON-LDの妥当性**（Programmatic SEO戦略の前提） — 中
6. **レスポンシブ・アクセシビリティ** — 中〜低（基本機能確立後に着手）

---

## 11. 未決事項（要確認・今後の判断が必要な点）

- E2Eテストで実際にOpenAI APIを叩く範囲（コストとのトレードオフ）は、CI予算確定後にE2E-5の実行頻度を再検討する。

> 補足: OpenAI Moderation API障害時の方針は詳細設計書9章で「フェイルクローズ（500を返し投稿失敗とする）」に確定済み。対応する結合テストは5.1章の#8に追加済み。
